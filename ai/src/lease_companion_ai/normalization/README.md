# normalization/

## 책임

추출된 원문 값을 규칙 엔진이 비교할 수 있는 정규 형식으로 변환한다. 이름·주소·금액·날짜를 표준화하되, **원문 값은 보존하고 정규화 값을 별도로 남긴다.** 판정은 하지 않는다.

## 하위 구조

- `names/` — 임대인·대리인·계좌 명의 등 이름 표기 정규화
- `addresses/` — 목적물·등기 주소 표기 정규화 (도로명·지번 대응)
- `money/` — 보증금·월세·계약금·잔금 금액 정규화, 숫자·한글 표기 병행 보존
- `dates/` — 지급일·입주일·계약기간 날짜 정규화

## 입력

- `extraction/` 출력: 문서별 원문 필드 값

## 출력

- 원문 값 + 정규화 값 쌍 (교차검증·표기 일치 판정용)
- 정규화 실패·모호 값은 플래그로 표시

## TODO

- 주소 정규화 기준(도로명/지번 매칭 소스) 확정 필요
- 금액 숫자-한글 표기 일치(J07) 비교 규칙과의 인터페이스 정의 필요
