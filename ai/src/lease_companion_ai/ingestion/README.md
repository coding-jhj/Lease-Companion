# ingestion/

## 책임

업로드된 계약 문서를 인식해 후속 추출이 쓸 수 있는 텍스트·레이아웃으로 변환한다. PDF는 직접 추출(PyMuPDF·PDF.js)하고, 스캔·이미지는 OCR(PaddleOCR-VL-1.6, VLM 통합)로 처리한다. 문서 종류를 판별하고 입력 품질을 점검한다. **추출·판정은 하지 않는다.**

## 하위 구조

- `pdf_parser/` — 텍스트 PDF에서 직접 텍스트·레이아웃 추출
- `ocr/` — 스캔·이미지 문서 OCR (PaddleOCR-VL-1.6, VLM 통합)
- `document_classifier/` — 계약서 / 등기사항증명서 / 건축물대장 등 문서 종류 판별
- `quality_check/` — 해상도·잘림·판독 가능성 점검, 저품질 시 재업로드 신호

## 입력

- 사용자가 업로드한 원본 문서 (PDF·이미지)

## 출력

- 문서 종류 라벨 + 페이지별 텍스트·레이아웃 + 품질 상태
- 판독 불가 시 `분석 불가` 시급도로 반환

## 확정 / TODO

- 확정(2026-07-14 선정표): 디지털 PDF=PyMuPDF·PDF.js, OCR=PaddleOCR-VL-1.6 (VLM 통합, 별도 VLM 단계 없음)
- 문서 종류 판별 기준·라벨 집합 확정 필요 (TODO)
