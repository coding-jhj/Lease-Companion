# routing/

## 책임

파이프라인 단계별로 어떤 처리 모델을 쓸지, 실패·저신뢰 시 어디로 넘길지 선택한다. 상용 LLM(Gemini 3.5 Flash 구조화·추출, GPT-5.6 Sol 재검토·생성) 우선 처리, 제공자 장애·할당량 시 fallback 경로를 결정한다. OCR·VLM은 상용 LLM(Gemini 3.5 Flash)에 통합되어 별도 단계가 없다. 로컬 7B는 (선택) 성능비교 실험 경로로만 라우팅한다(MVP 크리티컬 패스 아님). **판정·생성 자체는 하지 않고, 경로만 정한다.**

## 하위 구조

- `model_router` — 단계·입력 특성에 따라 처리 모델 선택 (추출·분류·재검토·생성 단계별)
- `fallback_policy` — 저신뢰·오류·근거 부족·제공자 장애 시 대체 경로·재시도·중단 정책

## 입력

- 단계 식별자 + 입력 특성 + `confidence` 신뢰도 + 제공자 상태

## 출력

- 선택된 모델·제공자 + fallback 결정 (재검토 대상, 대체 경로, 중단 여부)

## TODO

- 상용 LLM 확정(Gemini 3.5 Flash·GPT-5.6 Sol). 로컬 베이스 모델(선택 성능비교 실험)은 미정 → 라우팅 대상 확정 후 구현
- 저신뢰 임계값·fallback 정책 수치는 평가 후 확정 (임의 지정 금지)
- 이 디렉터리는 아직 `.py` 없음. 구현 전 스캐폴딩만.
