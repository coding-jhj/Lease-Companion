# data/raw/

원본 입력 데이터 보관 위치.

> **이 폴더의 실제 데이터는 Git에서 제외된다.** (`.gitignore`에서 `data/raw/*` 제외, 이 README만 예외)

## 규칙

- 실제 계약서·등기사항증명서·건축물대장 등 개인정보 포함 원본을 커밋하지 않는다.
- 원본을 직접 덮어쓰지 않는다. 가공물은 `data/processed/`에 별도로 남긴다.
- 공유가 필요한 경우 완전히 비식별화해 `data/sample/`에 둔다.
